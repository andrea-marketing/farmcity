<%= simple_form_for [ @producer, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review::AUTHORIZED_RATINGS, include_blank: false%>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
